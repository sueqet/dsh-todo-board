# Changelog

## 0.10.1

- **面板底部那条状态线不再是一个可折叠区块**，它常显在左下方。
  - 删掉了「面板」这个区块标题和它的折叠开关：一个要展开才能读的事实，比没有更糟 ——
    它会被折起来，然后靠"没显示"来撒谎。区块本身只剩下里面那条线。
  - 线上就两件事，固定在**左下方**：**版本号**（`v0.10.1`，用来确认浏览器加载的是哪一版）
    和**未验收数量**（有 AI 完成待验收时追加 `· 待验 N`）。
  - 布局上用 `margin-top:auto` 把它钉在面板底边，而不是跟在列表后面浮在中间；
    日志页打开时照旧不显示（与之前一致）。
  - 同一行右侧的两条链接**保留**：`清理已验收 N`（清空已完成列表）与 `Cordis Plugin`
    （打开 Cordis 面板）。它们是功能入口，不是状态，所以没跟着"栏"一起删掉。
  - **整板折叠成一行时（parked）行为完全没变**：仍是 `TODO · 未完成数 · 下一条待办 · 待验 N`。
  - 折叠状态存 `localStorage` 的键没换，老数据里的 `panel` 字段被忽略，不需要清理。
- 测试：`client-smoke` 断言"区块只剩三个、没有「面板」这个标题、状态线在**所有区块都折叠时依然在**"；
  `check-browser` 在真实 Chrome 里量"把卡片拉高后状态线贴在底边"（`gap ≈ 0`）与"卡片里恰好三个区块标题"；
  `check-row-guard` 增加第 29 个变异体（**把状态线重新变成可隐藏**），现共 29 个，全部被抓。
- README 的面板说明同步（四区块 → 三区块 + 常显状态线）。

## 0.10.0

两个面板侧的改动：**验收过的行真的离开队列**，以及**备注终于看得见、改得动**。
都只动浏览器半边（`client/client.js`），host 侧一行未改 —— note 早就存着、也早就随派发发出去，只是面板从没显示过。

- **新增：已完成列表**（在「待办列表」与「面板」两个区块之间）。
  - **勾上右勾（已验收），这一行就从待办列表移走、进入已完成列表** —— 是搬走，不是原地变灰：
    以前它只是被划掉、留在队列里，于是「还剩什么没做」永远要靠肉眼过滤。
  - 两个列表是同一个筛选范围的两半：一个行只在其中一边，所以这是一个**移动**，
    而不是同一行画两遍。计数也跟着走：区块标题上的数字就是各自的行数。
  - **最近验收的排在最上面**（按 `updatedAt` 倒序）。否则刚勾的那条会掉到列表末尾，
    看起来像被删了 —— 排序在这里是可发现性的一部分。
  - 已完成的行**不再属于队列**：没有拖柄、也不是拖放的落点（`dropOn` 读的是待办列表），
    但它仍然是完整的一行：圆勾可以撤销验收（行回到待办列表）、▶ 与 ✕ 照旧。
  - 单独的区块、单独的类（`.dshtb-donelist`）：折叠状态与另外三个区块一样存 `localStorage`；
    列表和已完成列表各自有独立的类，所以「找列表」的代码/测试不会顺手拿到另一半。
  - 待办列表空了但已完成里有东西时，提示改成「未完成的都清空了（已完成的在下面）」，
    不再说「还没有待办」—— 那会让人以为条目丢了。
- **新增：备注（note）的显示与编辑**。
  - **行内预览**：备注非空时显示在标题下方，保留换行（`white-space:pre-wrap`），
    **最多 2 行**（紧凑界面 1 行），超出部分视觉上折叠；鼠标悬停可看全文。
    折叠只影响显示：全文始终在 DOM 里，选中/复制一行拿到的仍是完整字符串。
  - **独立的编辑控件**：行尾的「＋ 备注 / ✎ 备注」点开是一个**多行 textarea**
    （自己的类 `.dshtb-notearea`，`Enter` 保存 / `Shift+Enter` 换行 / `Esc` 取消，另有「保存 / 取消」）。
    **没有塞进标题编辑** —— 标题是单行提交语义（`Enter` 即提交），一行代码复用就会把多行备注腰斩。
  - **不裁剪、不设长度上限**：保存时原样送出（不 `trim`、不切片）。测试里用 4000 字符的备注
    逐字节比对过。备注是派发时发给模型的上下文，面板在这里撒一点谎，用户就会在看不见真实上下文的情况下批准工作。
  - 一个类只对应一个控件：备注按钮、文本框、保存、取消各有各的类，没有复用折叠/图片/模式/皮肤的任何类；
    备注正文是 `div` 而不是按钮 —— 它是文字，视觉上也不该像能点的东西。
  - 长不可断词（粘贴进来的路径、日志）用 `overflow-wrap:anywhere` 换行，
    否则一行会把面板顶宽 —— 这条在真实浏览器里量的是 `scrollWidth <= clientWidth`。
- 测试：
  - `client-smoke` 新增 `done` 与 `note` 两段：验收后的行确实换了列表、撤销能回来、
    已完成的行不可拖动也不是落点、备注全文渲染、编辑走 `patch(id,{note})`、
    `Shift+Enter` 不提交、`Esc` 不发送、清空即保存空备注、超长备注逐字节一致。
  - `check-row-guard` 增加 3 个变异体（**note 不渲染**、**保存不发 patch**、**已验收的行留在待办列表**），
    现共 28 个，全部被抓。
  - `check-browser` 在真实 Chrome 里量新元素的计算样式：备注可读、保留换行、长词换行不撑宽、
    编辑控件可见、已完成列表与其中的行可读、**票据/素白夹 2 行、紧凑夹 1 行**
    （用 `scrollHeight > clientHeight` 证明"真的夹住了"，而不是只声明了 `-webkit-line-clamp`）。
- 三套皮肤都没有破版：素白沿用无边框列表内边距，紧凑只改字号与行数上限；皮肤规则仍然只作用于本面板根节点之下。

## 0.9.1

三件事一起做，因为它们是同一件事的三面：**让「AI 改板子要先问」从约定变成机制**。
前两件是安全，第三件是 0.9.0 之前就埋下的 bug。方案与实测记录：`docs/design-ai-dispatch.md` §9–§13。

- **修复：模型工具 `todo_board` 会因加载顺序而静默消失**（既有 bug，0.9.0 及以前）。
  - 症状很隐蔽：**面板一切正常，模型却完全没有这个工具**，系统提示里也没有 TODO 板那一段，
    没有任何报错。原因是模型工具与提示词段落用**急切读取**拿服务
    （`const tools = ctx.get('tools')` + `if (tools !== undefined)`），
    而插件行比 `tools` / `systemPrompt` 服务先加载时读到 `undefined`，
    于是**静默跳过、永不重试**。面板与两条路由用的是 `ctx.inject`（会等服务就绪），所以照常工作 ——
    这正是一直没人发现它的原因。
  - 现在两处都用 `ctx.inject([...])`：服务什么时候到都能注册，服务消失也会卸载。
  - 这个 bug 对自动接续是致命的：派发出去的新会话需要 `todo_board` 才能「勾完成 → 取下一條」。
- **新增：AI 写板子走 DSH 原生审批门**（`tools/pre-execute` → `{kind:'ask'}`）。
  - **只有 `list` / `done` / `reopen` 不问**。`done` / `reopen` 是模型自己的左勾
    ——「做完一条就勾掉、再取下一条」这条循环正是用户要的自动化，而它在
    `danger-full-access`（审批档 `never`）下会被**每次拒绝**，那条链子会当场断掉；
    而且 `pendingInDir` 把 `aiDone` 的行视为「不在队列里」，勾选本身启动不了任何任务。
  - **其余动作一律问**，包括**这一版还不认识的动作**：忘记加进白名单的写操作必须失败关闭，
    而不是从 `if` 的缝里漏过去。
  - 审批框里的说明会具体到「新增待办「标题」，模式 resume（这条会被自动派发执行）」
    —— 模式写在用户眼前，避免「批创建时顺手把自动执行也批了」。
  - fail-closed 是 harness 的，不是我们造的：没有审批服务、调用没有 agent、裁决不是
    `allowed-once`，三种情况都由 `dsh-tools` 拒绝，且措辞各不相同，模型分得清
    「人说不行」和「这里根本没有审批通道」。
  - **面板的写操作不走这道门**（它走 HTTP 路由，由浏览器会话授权），
    **定时派发与回合结束钩子也不走**（它们不是工具调用，且在回合外 ——
    `approval.request()` 在回合外直接抛错）。这条分界就是约定好的边界：
    **审批挡「改板子」，不挡「跑队列」**。
- **修复：板子路由的围栏只做了半扇门，本机进程可以绕过审批写板子**（0.9.0 的既有缺陷）。
  - 0.9.0 把 `isTrustedApiRequest`（Host/Origin）复刻了过来，**漏掉了它后面的
    `browserAuth.isAuthenticated`** —— 而漏掉的恰恰是挡住 shell 客户端的那一半。
    旁证：`GET /` 对裸客户端返回 **401**，我们的路由却返回 **200**。
    后果不止是绕过审批：0.9.0 起**日志正文也挂在这条路由上**，任何本机进程都能读到。
  - 现在 `refuseUntrusted` **优先调用 harness 自己的 `connection.requestRejection(request)`**
    （它把 Host/Origin 与浏览器鉴权两件事一起做），拿不到该服务时才退回复刻版
    （没有 `dsh-client-connection` 的部署仍要能用）；拿不到、抛错、返回不认识的东西，
    一律按拒绝处理。
  - **面板不受影响**：浏览器带的是 DSH 自己签发的 `dsh-auth-*` 会话 cookie（`Path=/`、
    `HttpOnly`、`SameSite=Strict`）。证据不是推理 —— GUI 自己的 WebSocket 通道
    `/api/remote.mux` 走的就是同一个 `requestRejection`（`dsh-api-gateway/lib/index.js:463`），
    面板能用就说明浏览器握有该 cookie。
  - 代价要说清楚：**从此「用脚本/curl 直接往板子里写」也不再可用**（那正是要堵的通道）。
    若面板因为 cookie 缺失被挡，日志里会写明原因和解法（用 `dsh web` 打印的带 token 地址重开页面）。
  - **面板自己也会说清楚这件事**：被挡时页脚不再是干巴巴的 `HTTP 401`，而是
    「浏览器没有 DSH 会话凭据。请用 `dsh web` 打印的那条带 token 的地址重开页面」
    —— 这是本版唯一新增的、用户真能撞上的拒绝，不该看起来像面板坏了。
- **测试：三套件补上，另加一个宿主侧变异检查**（`npm test` / `npm run guard`）。
  - `tools/smoke.mjs` 新增 `gate` 与 `conn` 两段：白名单逐项断言、未知动作必须问、
    模式出现在原因里、别人的工具原样放行；以及围栏「harness 优先、复刻兜底」的四条分支。
  - `tools/check-http.mjs` 用**真实 node:http 服务 + 裸客户端**演一遍那条绕过通道：
    同一条本机写请求，没有会话 cookie 时 **401 且板子确实没被改**，带上 cookie 时 **200**。
  - `tools/check-live.mjs` **重写**。它原来把 `systemPrompt`/`tools` 塞进 `ctx.get` 的手写映射里，
    于是 `ctx.inject` 改成「等服务」之后它必然失败 —— 那正说明它测的不是真东西。
    现在两个服务都在**插件行之后**通过 `ctx.reflect.provide` 提供（就是要复现那个竞态），
    并新增**真实 `ToolRuntime`** 上的审批门验证：读放行、无审批通道时写入被拒且模型收到我们的理由、
    `allowed-once` 放行、`rejected` 用 harness 自己的措辞拒绝。
  - `tools/check-fence-parity.mjs` 在原有 72 条对照之外，**钉住我们借用的那个方法**：
    `requestRejection` 必须同时做 Host/Origin 与浏览器鉴权。DSH 哪天把它减成一半，
    这条会失败并把人引回 §12 —— 否则我们的授权层会跟着一起悄悄消失。
  - 新增 `tools/check-host-guard.mjs`：五个变异体分别删掉审批门、把白名单写窄、
    让路由无视 harness 门、让抛错的围栏放行、把注册退回按加载顺序决定 ——
    **每一个都必须被上面某个套件抓住**，跑完逐字节还原 `lib/index.js`。
- **新增：模型终于够得到面板的写操作**（`update` / `reorder` / `dispatch`），三个都挂在审批门后面。
  - `update`：改标题 / 模式 / 目录 / 备注。**先整体校验再落笔** —— 一个字段不合法就整条拒绝，
    不做「好的那几个先改上」：否则模型和刚点了批准的用户都说不清到底改了什么。
  - `reorder`：传 `ids` 指定新的从上到下顺序。**只接受同一个目录内的待办**：面板按目录分组，
    而两个模式各自推动自己目录的队列，所以跨目录的列表会悄悄重排用户从没打算动的队列，
    看起来却只是「挪了一下」。未知 id、重复 id、跨目录一律整条拒绝且顺序不动。
    未列出的待办保持原有相对顺序跟在后面（复用面板的 `reorderTodos`）。
  - `dispatch`：立即派发一条待办，复用面板 ▶ 的 `runTodo` → `dispatch`，所以失败处理、
    图片能力校验、会话绑定全都是现成的。**只有「未派发」的能派发** —— 这与面板 ▶ 的启用条件一致，
    不另造一套规则：重复派发是重复劳动，已完成的根本不是活。
  - 这三条也进了白名单之外（默认就是要审批），所以接上它们的**同时**就接上了审批；
    审批框里会写清这一条**具体**要动什么（改哪几个字段 / 顺序里最上面将是谁 / 派发哪一条），
    而不是只说一句「AI 要修改待办板」。变异检查里各有一个变异体专门盯它们的守卫
    （非法 mode 半途生效、跨目录重排、重复派发）。
- **决策 E1 已定：`add` 保留 `mode`**（用户 2026-09-26）。
  E1 原本是「`add` 不再接受 `mode`，新建一律 `remind`」，被否掉的理由和最初那条一样：
  **用户自己也要能一次建出「续跑 / 新会话」的待办**，模型侧同样需要（否则建一条会自动跑的任务
  要先 `add` 再 `update`，白白两次审批）。所以 `add` 照旧接受 `mode`，安全性由审批门保证 ——
  审批框把模式写在理由正中央：`新增待办「…」，模式 resume（这条会被自动派发执行）`。
  这一版**没有**对 `add` 的 `mode` 做任何收紧。
- **修复：一次 `newSession` 派发会把目标会话写坏，使它永久打不开**（自 0.6.1 起就存在的 bug）。
  - 症状：派发出去的新会话**确实把活干完了**，但此后在 GUI 里打开它只会得到
    `stored session … is corrupt: … session event at seq 8 message has invalid content`。
    派发注入的第一条用户消息正好是 `seq 8`。
  - 原因：那条消息用**裸字符串**当 `content`（`noticeMessage(todoPrompt(todo), …)`），
    而 harness 的 `UserMessage.content` 必须是 `ContentBlock[]`
    （`dsh-llm/lib/types/message.d.ts`）；`dsh-session` 的持久化校验直接拒绝
    （`if (!Array.isArray(content)) throw … 'message has invalid content'`）。
    致命之处在于**这时事件已经落盘**：一条坏事件让**整个会话**读不出来，而不是只丢那一条消息。
  - 现在 `newSession` 路径改用**已有的正确构造器** `todoContent(todo)`
    （`resume` 路径一直在用它），并在 `noticeMessage` 里加了**形状断言**：
    传字符串当场抛错，宁可让这次派发失败、把行记进 `lost`，也不要静默写坏一条会话。
  - 顺带修掉同一行的第二个 bug：**这条路径把 `todo.images` 丢了**。
    它刚刚还在用 `modelAcceptsImages` 检查模型能不能读图，却从没把图片放进消息里；
    改用 `todoContent` 后图片作为 `{type:'image'}` 块一起发出。
- **修复：`create` 动作静默丢弃 `note`**（既有 bug）。`createTodo` 构造行时没把 `input.note`
  交给 `normalize`（工具 `add` 一直有），于是调用方传的备注被无声吃掉：行建出来是空备注，
  派发提示里也就没有上下文。现在两条入口一致。
- **测试：新增 `tools/check-session-format.mjs`**（已进 `npm test`）。
  - 它驱动**全部四条注入路径**（`resume/followup`、`resume/steer`、`newSession/followup`、
    以及带图那条），把每条消息交给**真正的** `adoptSessionEvent()` ——
    正是加载会话时做校验的那个函数。
  - 另有一条**对照用例**钉住旧写法：字符串 `content` 必须被拒绝。没有它，
    这个测试将来可能变得空洞（例如校验器变得不可达时仍会「通过」）。
  - 它第一次运行就抓到了我漏掉的一件事：`user/message` 还需要 `surfaceOp`，
    而且它在 content **之前**校验 —— 所以对照用例必须带上 `surfaceOp`，
    否则会因为「缺 marker」而不是「content 非法」通过，守卫就失效了。
  - `tools/check-host-guard.mjs` 增加一个变异体：把 `todoContent` 换回 `todoPrompt`，必须被抓。

## 0.9.0

- **后台日志：标题栏 `⚙` 按钮，在同一个浮窗内切到日志页**（不是新窗口、不进 DSH 设置页）。
  平时它是一个不打扰人的图标；**出错时上面亮一个小圆点**，打开日志即视为已读。要的就是这个：
  「平时不暴露给用户，出错时能给开发者调试线索」。
  - 级别 / 来源 / 关键字三种过滤；**至少保留一个级别**（全关掉的空列表会被误读成「没有日志」）。
  - **最新记录排在最上面** —— 打开它的理由通常就是刚坏掉的那件事。
  - **复制** / **导出 .txt**（带环境头部 + 「贴出去前请自行确认」的提醒）/ **清空显示**。
    清空**只清面板、不动主机记录**：诊断面上一个会删掉证据的按钮是个陷阱。
  - **日志正文只在视图打开时才随轮询返回**（`?logs=1&since=<sn>`），关闭时轮询负载与改动前**逐字节相同**。
    唯一的例外是 `logAlert`（一个 `sn` 数字 + 时间戳，不含任何文本）—— 没有它，圆点就只能等用户
    打开日志之后才亮，而圆点的意义恰恰是「你不用打开也知道出事了」。
- **为什么不能直接用 `ctx.logger.buffer`**：cordis 注册那个环形缓冲时**没有给 `levels`**，
  于是门控退到默认的 `INFO`，`warn(2)` 和 `debug(3)` 全被挡掉 —— 实测只收到 `error` / `info`。
  而本插件的失败路径大量是 warn。所以自注册**一个** exporter，并**不靠它返回的 disposer 摘除**
  （有两个 exporter 时那个 disposer 会删错人，实测），回收交给 fiber 卸载。
- **默认只记本插件**（`levels: { 'dsh-todo-board': 3, default: 0 }`）：自己的四个级别全进得来，
  别的插件只收 `error`。理由是脱敏调研里复现的几条泄密路径**都在别的插件的配置校验错误里**。
  `default: 0` 是**范围选择，不是安全边界** —— 泄密照样从 error 路径进来，所以脱敏无条件开。
- **脱敏是承重结构，不是加固**。密钥从来不是被某处不小心的 `logger.warn(secret)` 记下的，
  而是夹在**别人构造好的消息**里（通常是 Config 校验失败时的 `Error.message`）。落盘与显示**每一行都过**，
  导出路径**再过一遍**（那个文件是要贴给别人的）。覆盖：`~` 归位、控制字符、`sk-`/`gh*_`/`npm_`/`Bearer`、
  JSON 里像密钥的键、普通 `key: value`、以及 `credential ref "…"` 和 `but got …` 这两种
  **「值所在位置」**的回显（回显的是用户粘进去的任意字符串，**不保证有 `sk-` 前缀**）。
  - 规则写法是被测试逼出来的，不是 review 出来的：`credential ref` 必须排在通用键值规则**之前**
    （否则通用规则把 `ref` 当值吃掉、密钥留在引号里）；通用键值要求**真的 `:`/`=`**
    （照抄参考实现会接受任意空白，把 `apiKey expected string but got X` 变成乱码）；
    `bearer` 的值类要排除引号、`but got` 的值不能以 JSON 标点开头（否则整个配置 dump 被吞成一个 `***`，
    而那正是最该看清的一条路径）。
  - **已知未覆盖**：JSON dump 里、键名不像密钥、值也没有可识别前缀的密钥。风险是**具体的、可枚举的**，
    不是「日志都危险」。
- **落盘** `$DSH_HOME/todo-board/log.ndjson`：一行一条，**每次追加后**检查 256 KiB 上限并过半裁剪
  （只在挂载时裁剪会让长命进程的上限失效），文件权限 `0o600`；写失败**停用落盘但保留内存日志**，
  一个坏写不能让后续每条都失败。
- **补齐失败路径 —— 这才是本项的实质工作**。原先 host 半边只有 3 处 logger 调用，
  而至少 8 条失败路径只改状态、不留痕迹。现在：
  - `markLost()` 是**所有派发失败的唯一收口点**，日志加在那里而不是每个调用点，所以不可能漏。
    它记下待办 id、目录、模式、目标会话、**第几次失败**、原因。
  - 读板失败 / 文件结构不对 / 跳过坏记录、写板失败、preset 解析失败、建会话失败、
    `agents` 服务不可用、图片入库失败、图片被拒、新会话未归入工作区、定时派发失败、
    回合钩子失败、HTTP 500 —— 全部留痕。**建会话与 preset 失败额外传异常对象**：
    `markLost` 只拿到摘要文本，栈要在这里留。
  - 路由信任校验被拒也记 warn，**上限 20 条**（否则一个重试循环的页面会把别的都埋掉），
    含 host / origin / sec-fetch-site —— 否则「面板为什么 403」无法回答。
- **安全修复（既有缺口，本次改动之前就存在）**：本插件的两条路由原本**不在 DSH 的 `/api` 信任围栏内**
  —— 那道围栏是按通道注册的。实施前实测：不带任何凭据就能 `GET` 出整个板子（`/api` 同样的请求是 401），
  且 `Content-Type: text/plain` 的**跨站 POST 会直接执行动作**（`text/plain` 是 CORS 安全列表值、
  不触发预检，而路由原本不看 content-type）—— 也就是说**任意网页都能增删待办、甚至派发任务**。
  另外 DNS rebinding 可以读走响应。**日志落地前必须先补这个**，否则等于给一条无鉴权、
  可被跨站读取的路由挂上诊断信息。现在两条路由都走同一条规则（复刻 DSH 的 `Host` / `Origin` /
  `Sec-Fetch-Site` 校验），`trustedHosts` 从 `webRuntime` **按请求现读**（该服务由兄弟行提供，
  挂载时未必存在），读不到就退化为仅回环。
  - `Host` 是承重项，不是实现细节：明文 HTTP 下浏览器对**读**请求不带 `Origin` 也不带 Fetch-Metadata
    （那些头只发给可信目标），所以一个没有任何标记的请求仍可能是被 rebinding 的浏览器读取，
    而 `Host` 正是 rebinding 无法伪造的那个头。
- 新增 `tools/check-http.mjs`（并入 `npm test`）：用**真实 `node:http` 服务 + 裸客户端**验证围栏。
  这一层必须存在，因为 `fetch` 会按 URL 重写 `Host`，表达不了 rebinding 那个场景 ——
  而 rebinding 正是这条围栏存在的理由。
- 新增 `tools/check-fence-parity.mjs`（并入 `npm test`）：把**本插件的围栏**与 **harness 自己的围栏**
  都从各自源码里取出来，在 72 个 host/origin/fetch-metadata 组合上逐例对表。存在的理由是
  **"复刻会漂移，而且方向必然是更松"** —— 那是没有任何别的测试会发现的一类回归。
  已实测：把围栏某一行改弱，它立刻点名具体是哪个用例。
- 新增 `tools/check-live.mjs`（并入 `npm test`）：用**真实 cordis 运行时**验证两件 stub 证明不了的事。
  `tools/smoke.mjs` 的 `inject` 是直接调回调，而真实 cordis 要等服务被 `ctx.reflect.provide` 出来才唤醒
  —— 如果这个语义差有影响，**两条路由在真机上根本不会注册，而所有 stub 测试照样全绿**。
  同理，stub 的 logger 是凭记忆重写的 `levels` 门控，真实 `LoggerService` 才是决定
  「我们自己的 debug 到底进不进得来」的那一个。实测两者都成立。
  教训：**stub 越多，「全绿」能证明的东西越少**，关键语义要有一个真实运行时的套件兜着。
- **自查抓到并修掉一个自己写出来的真 bug**：日志页的轮询装在**只跑一次**的 effect 里（空依赖），
  所以它闭包捕获的是**首次渲染**的 `logOpen` —— 永远是 `false`。后果是打开日志页后**只有那一次 fetch 生效，
  之后 2.5s 轮询永远不会再更新它**，看起来就像「日志页能用，只是再也不出新东西」。
  改为用 ref 读取当前值（那种只装一次的定时器只能这样看到最新状态），并补了两条断言：
  「打开时轮询会持续取日志」（必须能从 stale closure 失败）+ 一条对应的变异体（第 24 个）。
- `tools/check-browser.mjs` 增加**日志页**的计算样式检查（13 项）：行可见、能换行（栈需要）、等宽、
  error 与 warn 靠颜色区分、级别开关的选中态看得见、页面不溢出面板，以及**标题栏三个控件是三个不同元素**。
  最后这条是真实的约定：日志按钮若复用 `dshtb-fold`，`querySelector('.dshtb-fold')` 会返回"碰巧排在前面的那个"，
  折叠控件从此含义不明 —— 实现时确实这么写过，被既有的 `client-smoke` 断言抓了出来。
  这一层必须用真实浏览器，因为**一个文字被压成不可见、或被裁掉的日志页，和「什么都没记」长得一模一样**，
  而那正是这个功能唯一不能给出的答案。
- 测试：host 半边新增 `trust` / `logs` / `redact` 三组；browser 半边新增 `logs` 组
  （含「板子视图从不请求日志正文」「圆点会清掉」「最后一个级别关不掉」「轮询不会因 stale closure 停摆」）；
  变异体从 19 增到 **24**。
  测试当场抓出五个真问题：`credential ref` 漏网、通用规则吃掉诊断文本、`but got` 吞掉 JSON dump、
  `bearer` 吃掉 JSON 收尾引号，以及日志页轮询的 stale closure。
- 套件顺带修正：`smoke` / `retry-loop` 的假请求补上真实浏览器必然携带的 `host`，否则会被新围栏挡下
  —— 那本身就是围栏生效的证据。

## 0.8.0

- **截图直接 `Ctrl+V` 粘贴成附图**，新增框里的「📎 图片」选文件按钮**已删除**。截图本来就是图片，先存成文件、再点按钮、再在对话框里把它找出来，是绕远路。
  - **纯文本粘贴完全不受影响**：只有剪贴板里真的带图片时才 `preventDefault`。粘文字进输入框是写待办的主要方式，不能为了一个附带功能把它弄坏 —— 这条有专门的变异体守着（「文本粘贴被吞掉」必须让测试失败）。
  - 一次粘太多会**粘满为止并告知**（「只粘贴了前 N 张（上限 4 张），其余已忽略」），不是静默丢弃也不是整批拒绝。已经满了再粘会明说原因。
  - **剪贴板图片常常不带 MIME 类型**（部分截图工具如此）。这类会按文件名的扩展名补上类型，且只接受 host 认可的那四种；扩展名不认识的（比如 `.bmp`）**不当图片处理、不拦截粘贴**，而不是转发给 host 去失败。host 对媒体类型是严格校验的，`type` 为空会被它当成「(未声明)」直接拒收。
  - 剪贴板的两个面都读：`files` 和 `items[].getAsFile()`。有些粘贴只在其中一个面上出现，只读一个会漏。
  - 支持与输入框混用：同一份剪贴板里既有文字又有图片时，图片照样附上。
  - **空着时不占地方**：原先附图区常驻一行说明文字（「截图后在这里 Ctrl+V 直接粘贴成附图（最多 4 张，PNG/JPG/WebP/GIF）」），已删除。能力由 placeholder 里的 `Ctrl+V 粘贴截图` 说明；附图区只在真的附了图之后才出现。**格式支持与数量限制不变**，仍由粘贴逻辑强制，并在触发时给出提示 —— 删掉的只是那行字。同时保留 `已附 N / 4 张` 计数：附图区出现后，那是「还剩几张」唯一的屏上说明。
- 新增 `tools/check-browser.mjs`（`npm run test:browser`）—— 原先的 `check-skin-css.mjs` 扩成通用浏览器自检，因为它和**平台假设**属于同一类事。Node 套件把剪贴板对象当参数传进去，验证的是插件**逻辑**，而默认了剪贴板的**形状**；形状是浏览器的事，也正是会无声变掉的东西。现在用真实的 `DataTransfer` / `File` / `ClipboardEvent` / `FileReader` 逐条确认：
  - 图片粘贴会填 `clipboardData.files`，`items` 里有 `kind: 'file'`，`getAsFile()` 能取回文件；
  - **文本粘贴的 `files.length` 是 0、`items` 里没有 file** —— 这正是「不拦截文本」得以成立的前提；
  - `FileReader` 的 data URL 真的有逗号可切、前缀确实是 `data:image/png;base64`；
  - `new File([...], name, { type: '' })` 确实报空 `type`（扩展名兜底就是为它写的）。
- 测试补齐：删掉那行说明后，**四种格式（PNG/JPG/WebP/GIF）逐个验证仍被接受且媒体类型不被丢**，大小写与 `jpeg` 四字母扩展名的兜底也各测一遍 —— 因为那行字曾是唯一提到这四种格式的地方，删它正是最可能把支持一起带走的改动。
- `tools/check-row-guard.mjs` 增加 7 个变异体：文本粘贴被吞、图片粘贴不拦截、空媒体类型不兜底、粘贴时上限失效、非图片文件被误吃、删掉的那行说明又回来、附了图之后计数消失 —— 实测 19 个变异体全部被抓。

## 0.7.0

- **三套可切换的界面**，标题栏的 `▤` 循环切换，选择存 `localStorage`（`dsh.todoBoard.skin.v1`），刷新与折叠状态下都保持：
  - **票据 `ticket`**（默认，即原来的样子）→ **素白 `plain`**（去边框去分隔线、留白更多，状态收成一个色点，只有「进行中」和悬住的行留淡底）→ **紧凑 `dense`**（结构同票据，字号行距各降一档，一屏更多条）。
  - 按钮上的字形**就是**当前那套，tooltip 说明它改了什么 —— 之前想的是「设置页选」或「面板里放一排」，最后选了循环按钮：三套风格下，循环不需要菜单、不需要额外的行，而且它就长在你本来就会看的那条标题栏上。
  - **切换只动外观**：控件一个不多一个不少，含义也不变（方形=AI 已完成、圆形=已验收，三套一样 —— 那是含义不是装饰）。数据不动，DSH 其他界面也碰不到：每个皮肤规则都限定在 `.dshtb-root[data-dshtb-skin="…"]` 之下。
  - 皮肤是**只加不改**的样式表：基础表描述 `ticket`，另两套只写差异。所以某条声明写错时退回去的是「原来那套界面」，不是「没有样式」。
- 新增 `tools/check-skin-css.mjs`（`npm run test:render`）：**真实浏览器**渲染自检。另外两个套件只能断言 CSS 文本，而一条规则可以文本正确、作用域正确，却在层叠里被基础样式表压过去 —— 那种失败在字符串测试里和「正常工作」长得一模一样，在点击的人眼里则和「切换没反应」一模一样。这个检查用 headless Chrome + CDP 量计算样式，是唯一能区分「生效了」和「只是存在」的。
  - 它抓到的两件事：`plain` 的卡片边框是**透明**而非零宽（刻意的 —— 换皮肤不该让布局跳一下），以及「无边框」可以用两种写法表达，检查应该问可见性而不是问某种写法。
- 自查发现并修掉一个自己写出来的真 bug：`plain` 皮肤原本给区块标题和分组标题写了 `font-size:0`，那会把「新增待办」和目录名**整个抹掉** —— 而树遍历测试看不见这种消失（节点还在、文本还在、只是渲染为空）。改小而不是改没，并加了一条断言专门守这类写法（`skintext OK`）。
- `tools/check-row-guard.mjs` 增加 5 个变异体：皮肤选择不落盘、切换够不到样式表挂钩的属性、皮肤抹掉区块标题、皮肤规则逸出面板、两套可选皮肤只注入一套 —— 实测 11 个变异体全部被抓住。

## 0.6.1

- 修复：**面板上看不到待办状态**。0.6.0 加了状态推导，但只在「需要你注意」时才画标签 —— `未派发` 和 `已完成` 被有意留空（当时的理由是空勾选框和划线标题已经说明了）。实际效果是**大多数行根本没有任何状态显示**，这恰恰是状态标签要回答的问题。
  - 现在**每一行都带且只带一个状态标签**：`未派发` / `已派发` / `进行中` / `已完成` / `目标会话丢失`。
  - 强调程度仍然分级，所以一屏普通待办不会吵：只有 `进行中`（蓝）和悬住（红）显眼，`未派发` 稍暗，`已完成` 用绿字但不加边框。
  - `已完成` 的划线仍只跟**验收**走（那是你自己勾的），所以 AI 勾完但你没验收的行会显示「已完成」但依然清晰可读。
- 排查结论记录：host 侧一切正常（重启后 `/dsh-todo-board/api` 正确返回 `state`/`lostAt`/`lostKind`/`targetAlive`），面板页脚也确认浏览器跑的是 v0.6.0 —— 所以这不是投递问题，是 0.6.0 的显示策略问题。
- `tools/check-row-guard.mjs` 增加两个变异体专门守这条：让 `pending` 或 `done` 行重新「静默」，测试必须失败（实测都能抓到）。

## 0.6.0

- **待办状态**：host 侧从实时会话列表推导每条待办的状态，面板只在需要你注意时显示标签：
  - `未派发`（无标签，▶ 可点）/ `已派发`（绿）/ `进行中`（蓝，目标会话此刻正在跑）/ `已完成`（划线）/ `目标会话丢失`（红）。
  - 状态是**推导**出来的，不落盘：`进行中` 取决于 agent 的实时 status，任何文件都存不住；落盘的状态字段还会和时间戳本身漂移。
  - 「悬住」的原因分门别类（`lostKind`）：`目标会话丢失` / `图片被拒` / `建会话失败` / `预设解析失败` / `服务不可用`。图片被拒时不会谎称「目标会话丢失」——会话好着呢，只是模型看不了图。
  - `todo_board list` 的输出同样带上「状态=」一列，`state` 也进了输出 schema。
- 修复：**目标会话丢失的待办会静默卡死**。旧代码在派发失败（找不到可接续的活动会话）时依然盖上 `dispatchedAt`，而这个戳正是调度器和回合结束钩子用来跳过一条待办的依据 —— 于是一条定时待办在源会话关闭后，会在到点时失败、被标记「已派发」、然后**永远不再重试**，界面上也不报错。现在失败**不盖**这个戳，改记 `lostAt`，并按退避自动重试（1 分钟起，逐次翻倍，上限半小时）；改模式 / 换目录 / 解绑会话都会立刻清掉标记重新排队。卡住的待办也不会挡住队列后面的条目。
- 修复：**派发失败会把 host 卡死**（写测试时发现，与本 issue 无关的既存 bug）。`armTimer()` 在有待办到期时会调 `fireDue()`，而 `fireDue` 又从派发的 `.finally()` 里重新 `armTimer()`。如果一条到期的待办派发失败且**什么都没记**（既没 `dispatchedAt` 也没 `lostAt`），它就永远「到期」，重试链会**纯靠微任务自我循环**：事件循环一次都跑不到，计时器永远不触发，整个 host 冻结。已复现：200 次重试、全程不让出事件循环。现在**所有**失败路径（找不到会话、新建会话失败、preset 解析失败、图片被拒）统一走 `markLost()`，退避让重试收敛。`tools/retry-loop.mjs` 是回归守卫 —— 它会在循环复发时同步中止并报错（实测：把退避去掉后它确实失败，恢复后通过），已并入 `npm test`。
- **▶ 只在「未派发」时可点**：已派发、进行中、目标会话丢失的待办一律禁用，鼠标悬停会说明原因。重复派发只会产生重复工作，而无处可去的待办应该先改模式或换目录，而不是被塞给「当前恰好开着的」那个会话。
- 修复：新增框的「目录」一行不再让暗色的 placeholder 冒充已填好的值。旧文案 `目录：<cwd>` 看起来像「已经填好了」，实际输入框是空的（很多人第一眼都读成了默认值）。现在有可见的「目录」标签，placeholder 直接写清留空的行为（`留空 = <cwd>`），tooltip 说明这个值真正决定什么：分组、筛选、以及回合结束后谁来接续。
- `lostReason` / `lostAttempts` 随面板记录下发，面板的红标签 tooltip 会点名是哪个会话不见了、已自动重试几次。
- 修复（issue #1 第 2 条）：**窗口变窄会把面板裁到屏幕外**。位置按绝对像素存在 `localStorage`，而夹紧只在拖动/缩放时执行，窗口 resize 时没有任何代码重算 —— 视口缩到存储的 `left` 以下，面板就被裁掉，且点不到（能点的东西本身在屏幕外）。现在两处都补上：
  - 窗口 resize（含 `visualViewport`，覆盖浏览器缩放）时重新夹紧；
  - 加载时夹紧一次，所以光刷新页面就能救回被旧会话或大显示器存歪的布局，不必手清 `localStorage`。
  - 用户的**选择**与**当前渲染**分开保存：夹紧只影响显示、不落盘，所以窗口再拉宽时面板回到你放它的地方（与刷新后的结果一致），也不会因为一次缩窗就永久黏在角落。双击标题栏还原依旧有效。
  - `tools/check-clamp-guard.mjs` 用变异测试确认这条守卫真的会失败：分别去掉 resize 监听与加载时夹紧，测试都必须挂（实测两者都能抓到，恢复后通过）。
- 面板：**同一个事实不再在一行里说两遍**。
  - 目录只保留标题下小字里的完整路径（`目录 D:\...`），行下方的目录标签去掉了 —— 它只是把小字里的目录名又说了一遍。
  - 定时反过来：只保留行下方的定时标签，小字里的「定时 / 不定时」去掉了。原标签尾部那个 `✕` 独立成一个同级小按钮（按钮不能嵌套控件）。
- 面板：**已在列表里的待办现在也能改定时**。
  - 没定时的行显示 `🕓 不定时`，点一下就地展开时间选择器；已定时的行点时间标签也是同一个选择器，会带着当前时间打开，可直接改。
  - `Enter` 或 ✓ 保存，`Esc` 取消；保存走和新增框完全相同的 `patch` 通路，清空即取消定时。
- `tools/check-row-guard.mjs`：对上面两条做变异测试（恢复目录标签 / 把小字里的定时加回去 / 去掉「不定时」入口 / 让保存不发请求），4 个变异体全部被抓到，且 `client.js` 逐字节还原。`npm run guard` 一次跑完两个守卫。

## 0.5.0

- **面板可以折叠**，不用的时候只占一行：
  - 标题栏的 `–` 把整板收成**一行**（不再是浮动胶囊）：`TODO · 未完成数 · 下一条待办` + 待验收计数；点这一行任意位置展开，键盘 `Enter` / 空格也行。这一行按内容收窄，不撑满面板宽度。
  - 面板内部三个区块各自可折叠：**新增待办**（输入框 / 图片 / 模式 / 定时 / 目录）、**待办列表**（标题带条数）、**面板**（页脚计数器 / 清理已验收 / Cordis 入口）。区块标题本身就是开关，箭头 `▾ / ▸` 表示状态。
  - 折叠状态存进 `localStorage`（`dsh.todoBoard.sections.v1`），刷新后保持。
- 修复：标题栏里的按钮点不动。拖动用的 `setPointerCapture` 会把随后的 click 重定向到指针捕获元素，按钮永远收不到点击；现在标题栏上的 `pointerdown` 落在按钮上时不启动拖动。（收成一行后如果没有这个修复，会卡在收不起也展不开的状态。）
- `tools/client-smoke.mjs`：浏览器半边的自检（自带极简 React hook 运行时 + DOM stub，无需浏览器），覆盖区块折叠、一行视图的内容、以及标题栏按钮不被拖动吞掉。`npm test` 现在跑 host + client 两个 smoke。

## 0.4.1

- 市场素材：`assets/screenshot-1.png`（整体界面）与 `assets/screenshot-2.png`（面板特写）由 `tools/capture-screenshots.mjs` 从真实运行中的 UI 渲染生成，配 `screenshots.json` 声明；README 顶部展示面板截图。

## 0.4.0

- **待办可以带图片**：图片存进 DSH 的附件库（走和聊天上传同一条 `attachments.saveImage` 通路，限制、媒体类型、归一化都用 harness 自己的），派发时作为**真正的 image 块**和提示词一起发给模型——不再是"文件名写在文字里"。
  - 面板：新增框有「📎 图片」按钮（最多 4 张，PNG/JPG/WebP/GIF），带缩略图预览、逐张移除；行上显示缩略图，点击放大，✕ 单张移除。
  - 图片字节由插件自己的 `GET /dsh-todo-board/image?id=…` 提供：harness 自带的图片读取是**会话作用域**的，只认会话日志里引用过的附件，而待办的附件存在板上——所以板自己当权威，未被任何待办引用的 id 一律 404。
  - `todo_board`：`add` 支持 `images`（主机上图片文件的绝对路径，host 侧读盘并入库），`list` 标注图片张数，输出 schema 增加 `imageCount`。
  - 派发前会检查目标模型是否声明了 image：文本模型（比如 `deepseek-v4-flash`）会直接给出「目标会话的模型不接受图片输入」，而不是等适配器抛 `UNSUPPORTED_CONTENT`。
  - 附件不阻塞会话：`attachments` 服务缺失、格式不支持、超过 4 张都会得到明确错误，会话照常创建。
- 修复：`sendTo` 改成同步写 `dispatchedAt` + `nextDue` 跳过在途派发——图片能力探测引入了 await，否则定时器会在派发完成前把同一条待办再挑一次，导致 `fireDue` ⇄ `armTimer` 无限递归。

## 0.3.1

- **新会话归入工作区**：`自动新会话` 创建的会话现在会被登记到所属工作区（`workspaceRegistry.resolveByPath(dirPath)` + `attachSession`），侧栏不再把它列为「未分组」。分组是工作区记录上的 `sessionIds`，不是会话头字段——DSH 自己开会话时会 attach，而 `agents.create` 不会。
- 分组失败不影响会话：目录为空、没有 workspaceRegistry 服务、或 attach 被拒（比如 cwd 校验不通过）时只记一条 warn，会话照常创建并派发。
- 测试新增：断言新会话被 attach 到待办目录对应的工作区。

## 0.3.0

- **新会话绑定**：`自动新会话` 的待办在第一次派发后把创建的会话 id 记到自己身上（`runSessionId`），之后 ▶ 或回合结束自动接续都**复用这个会话**，不再一次运行开一个会话；只有绑定失效（会话已不在）或用户手动解绑，才会再开一个新的。
- 面板：`自动新会话` 行显示绑定的会话标签（点 ✕ 解绑）；`runSessionId` 可通过 `patch` 接口清除或改写。
- 面板：每行标题下多一行等宽只读信息 `目录 … · 定时 …`，窄面板里也一定能看到工作目录与执行时间，不再只靠小标签。
- 面板：页脚加构建标记 `v0.3.0`，用于确认浏览器加载的是哪一版客户端。
- 面板：工作目录标签的悬浮提示给出完整路径（或明确说明「未绑定目录」）。

## 0.2.0

- **定时执行**：每条待办可带一个本地时间（`YYYY-MM-DDTHH:mm`），到点前不派发，到点后按自身模式执行。
  - Host 侧一个自我重排的定时器 + 30s 兜底 tick（笔记本休眠丢掉的 timeout 靠它补上），两者都靠 `dispatchedAt` / `remindedAt` 幂等。
  - 回合结束钩子会跳过**未到点**的待办，不再把定时任务当成普通下一条提前跑掉。
  - 面板新增「定时」时间选择器、行内定时标签（已到点变黄，点击取消）、到点桌面通知（提醒模式）。
  - `todo_board` 工具新增 `schedule` action，`add` 支持 `schedule` 参数，`list` 输出标注「未到点 / 已到点」。
- 修复：面板新建的待办没有 `runSessionId`，定时派发会落到「没有可接续的活动会话」；现在会回退到 `sourceSessionId`。
- 修复：`newSession` 模式先于会话查找判定，不会再被来源会话抢先接续。
- 所有板写入都经由 `afterChange()`（save + 重新定时），避免定时目标被改动后计时器还指着旧时间。

## 0.1.0

首个版本。

- 右上角浮窗（`shell.overlay`）：按工作目录分组，分段筛选（当前目录 / 当前会话 / 全部，带计数）。
- 每条待办三档执行模式：提醒 / 自动续跑（注入当前会话）/ 自动新会话（新建同目录会话）。
- 双勾选：左勾「AI 已完成」由模型调用 `todo_board action=done` 自动勾选，右勾「已验收」只有用户能勾。
- 模型工具 `todo_board`：`list / add / done / reopen / note`，配合系统提示段落让模型干完一项就查下一条。
- 回合结束钩子 `agent/turn-stopping`：按列表从上到下的顺序派发同目录下一条未派发的自动模式待办。
- 面板可拖动、可缩放，位置与尺寸持久化到 `localStorage`；双击标题栏还原。
- 多行输入框（自动增高），`Enter` 添加、`Shift+Enter` 换行。
- 行内编辑：双击标题改名，点模式标签循环切换。
- 防重复派发：一条待办派发过一次即标记「已派发」，每回合最多派发一条。
- 「自动新会话」通过 `ctx.root` + `agentPresets.mount()` 创建，会话归属应用根上下文，与插件生命周期解耦。
- 侧边栏底部的 `Cordis Plugin` 入口折起，入口移到本面板底部；打开的 Cordis 面板显示在本面板正下方（右对齐、不重叠），并随本面板的拖动/缩放移动。
